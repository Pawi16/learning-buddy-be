type: object
required:
  - course_id
  - quiz_topics
properties:
  course_id:
    type: integer
    description: "ID of the course (for ownership validation)"
  quiz_topics:
    type: array
    items:
      type: object
      required:
        - topic_id
        - quiz_config
      properties:
        topic_id:
          type: integer
          description: "ID of the Topic (from Postgres)"
        quiz_config:
          type: array
          description: "List of difficulty settings for this topic"
          items:
            type: object
            required:
              - difficulty
              - quiz_type_config
            properties:
              difficulty:
                type: string
                enum: [EASY, MEDIUM, HARD]
              quiz_type_config:
                type: array
                items:
                  type: object
                  required:
                    - type
                    - number
                  properties:
                    type:
                      type: string
                      enum: [NORMAL_MULTIPLE, STATEMENT_VERIFICATION, STATEMENT_COUNTING]
                    number:
                      type: integer
                      example: 3